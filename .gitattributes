# Sunucu betikleri ve systemd/nginx birim dosyaları Linux'ta çalışır → DAİMA LF.
# (CRLF ile checkout edilirse "#!/usr/bin/env bash\r" bad interpreter hatası verir.)
*.sh        text eol=lf
*.service   text eol=lf
*.timer     text eol=lf
*.conf      text eol=lf
